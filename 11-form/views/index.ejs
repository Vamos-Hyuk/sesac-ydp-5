<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>폼</title>
  </head>
  <body>
    <h1><%= title %></h1>
    <h2>GET 요청</h2>
    <form action="/getForm" method="get">
      <input
        type="text"
        name="id"
        placeholder="ID는 최소 3글자"
        required
        minlength="3"
      />
      <input
        type="password"
        name="pw"
        placeholder="PW는 최소 4글자"
        required
        minlength="4"
      />
      <input type="email" name="email" placeholder="EMAIL" />
      <input type="number" name="grade" placeholder="GRADE" />
      <button>제출</button>
    </form>

    <h2>Post 요청</h2>
    <!-- reqiured 속성: 폼 요소에 값이 반드시 들어가야하는지 여부를 결정  -->
    <!-- pattern 속성: 폼 요소에 값 입력되었을 때 그 입력된 값에 대한 검증을 의미 -->
    <form action="/postForm" method="post">
      <input
        type="text"
        placeholder="ID"
        required
        pattern="^([a-zA-Z0-9가-힣]){4,}$"
        title="아이디는 대소문자, 한글, 숫자로 구성된 4자 이상의 문자여야 합니다."
        reqiured
      />
      <input
        type="password"
        placeholder="PW"
        pattern="^([a-z0-9]){8,12}$"
        title="비밀번호는 영어 소문자와 숫자로 구성된 8자리 이상 12자리 이하의 문자여야 합니다."
        required
      />
      <button>제출</button>
    </form>

    <form action="/getForm2" method="get">
      <p>이름 <input type="text" name="text" /></p>
      <fieldset class="">
        <legend>성별</legend>
        <input type="radio" name="gender" id="Male" value="Male" /><label
          for="M"
          >남자</label
        >
        <input type="radio" name="gender" id="Female" value="Female" /><label
          for="F"
          >여자</label
        >
      </fieldset>
      <br />
      <fieldset class="">
        <legend>생년월일</legend>

        <select name="year" id="year"></select>
        <label for="year">년</label>

        <select name="month" id="month"></select>
        <label for="month">월</label>

        <select name="day" id="day"></select>
        <label for="day">일</label>
      </fieldset>
      <br />
      <fieldset class="">
        <legend>관심사</legend>
        <input
          type="checkbox"
          name="interest"
          id="tennis"
          value="tennis"
        /><label for="tennis">테니스</label>
        <input
          type="checkbox"
          name="interest"
          id="soccor"
          value="soccor"
        /><label for="soccor">축구</label>
        <input
          type="checkbox"
          name="interest"
          id="develop"
          value="develop"
        /><label for="develop">개발</label>
        <input
          type="checkbox"
          name="interest"
          id="youtube"
          value="youtube"
        /><label for="youtube">유투브</label>
        <input type="checkbox" name="interest" id="music" value="music" /><label
          for="music"
          >음악</label
        >
      </fieldset>
      <br />
      <fieldset>
        <legend>기타</legend>
        <span>좋아하는 색상</span>
        <input type="color" name="fav_color" />
        <br />
        <span>좋아하는 숫자</span>
        <input type="number" name="fav_number" />
      </fieldset>
      <br />
      <button type="submit">회원가입</button>

      <script>
        // 년 옵션 생성
        var yearSelect = document.getElementById('year');
        var currentYear = new Date().getFullYear();
        for (var year = 1950; year <= currentYear; year++) {
          var option = document.createElement('option');
          option.value = year;
          option.text = year;
          yearSelect.appendChild(option);
        }
        // 월 옵션 생성
        var monthSelect = document.getElementById('month');
        for (var month = 1; month <= 12; month++) {
          var option = document.createElement('option');
          option.value = month;
          option.text = month + '월';
          monthSelect.appendChild(option);
        }
        // 일 옵션 생성
        var daySelect = document.getElementById('day');
        for (var day = 1; day <= 31; day++) {
          var option = document.createElement('option');
          option.value = day;
          option.text = day + '일';
          daySelect.appendChild(option);
        }
      </script>
    </form>
    <br />
    <br />
    <a href="/">홈으로 이동 </a>
  </body>
</html>
